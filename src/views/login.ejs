<div class="card">
  <h2>Login</h2>
  <p class="muted">Enter the Mossbox password.</p>
  <% if (error) { %><p><strong><%= error %></strong></p><% } %>
  <form method="post" action="/login">
    <input type="password" name="password" placeholder="Password" required />
    <button type="submit">Login</button>
  </form>
</div>
